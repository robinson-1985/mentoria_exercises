/* 5.11  Escreva um programa que pergunte o depósito inicial e a taxa de juros de 
uma poupança. Exiba os valores mês a mês para os primeiros meses. Escreva o total 
ganho com juros no período. */

#include<stdio.h>
int main() {
    float deposito_inicial, taxa_juros, total_ganho;
    printf("Declare o valor do depósito: ");
    scanf("%f", &deposito_inicial);
    printf("Declare a taxa de juros da poupança: ");
    scanf("%f", &taxa_juros);

    

    getchar();
    return 0;
}